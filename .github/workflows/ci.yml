name: CI

on:
  pull_request:
    branches:
      - main
  push:
  workflow_dispatch:

permissions:
  contents: read

# Keep CI cost-conscious by cancelling older runs for the same branch.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  repo-checks:
    name: Repository Checks
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify required project docs exist
        run: |
          test -f README.md
          test -f AGENTS.md
          test -f .gitignore

      - name: Verify Python cache files are ignored
        run: |
          grep -q '^__pycache__/' .gitignore
          grep -q '^\.pytest_cache/' .gitignore

  python-mvp:
    name: Python MVP Quality Gates
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install project and test dependencies
        run: pip install -e ".[dev]"

      - name: Lint with Ruff
        run: ruff check .

      - name: Compile Python sources
        run: |
          for path in apps brand_os director_os interview_os one_on_one_os packages scripts tests; do
            if [ -d "$path" ]; then
              python -m compileall "$path"
            fi
          done

      - name: Run test suite
        run: pytest --cov=. --cov-report=term-missing --cov-report=xml

      - name: Run Director OS local evaluation set
        run: python scripts/run_director_os_evals.py

      - name: Run Director OS multi-agent evaluation set
        run: python scripts/run_director_os_evals.py --cases-path evaluations/director_os/multiagent_cases.json

      - name: Run Brand OS local evaluation set
        run: python scripts/run_brand_os_evals.py

      - name: Run Interview OS local evaluation set
        run: python scripts/run_interview_os_evals.py

      - name: Run One-on-One OS local evaluation set
        run: python scripts/run_one_on_one_os_evals.py

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
