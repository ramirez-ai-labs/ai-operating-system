# Base URL for the AI-OS FastAPI backend.
# The backend must be running before starting the Next.js dev server.
# Start the backend: uvicorn apps.api.main:app --reload --env-file .env
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
